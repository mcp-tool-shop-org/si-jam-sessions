[package]
name = "score-model"
version = "0.1.0"
description = "The integer-only interface between score ingest and the si-jam-sessions law."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
