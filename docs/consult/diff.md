# Consult diff — si-jam-sessions phase-0 lock (2026-09-25)

Reviewers: Claude (this advisor seat, Anthropic) and Gemini (`google/gemini-3.1-pro-preview` via OpenRouter, served by Google, prompt sha256 473ccae9…, $0.095). Lock author: Grok (xAI), not a reviewer.

| | Claude | Gemini | Read |
|---|---|---|---|
| Verdict | revise | revise | agree |
| Q1 feel in the law? | Yes: a stepping transport with a commit horizon, and a take (integer sample onsets citing score note ids) | Yes: tempo map or micro-timing offsets | agree on the premise; Claude's is concrete and still all-integer |
| Q2 corpus | Shelf not ten (OpenScore CC0, gated PDMX); add edition-year and in-file-licence clauses; ≥37 paired held-out items from ≥10 works; CC-BY own tier; SA refused | "Several thousand pieces" needed; CC-BY admitted with ledger | disagree on scale; Gemini's figure is unsourced and conflicts with the sibling's own powered win at 36 records |
| Q3 checkers | All predicates; copy check vs whole corpus; teaching = typed claims + template | All predicates; teaching refused | near-agree |
| Q4 missing sound | The player's own note against the beat (a take), plus a click | A metronome or drum track | partial: both want time context; Claude says the take is what makes it worth doing |
| Q5 clock field | `onset_sample: u64` + `note_id` on every note-on | tempo map / tick-to-µs events as integers | agree in substance |
| Q6 instead | Score + constructed take + per-note verdicts + click; golden over all three | A two-turn host loop + metronome | both reject "one score + a hash" |
| SCAR | A played take is not picture | A rigid integer law cannot carry expressive feel | Claude's Q1 answers Gemini's scar: feel as integers in the law |
| Evidence | Every BASIS a page opened this session or a sibling file | Every BASIS "speculation" or "[recall]"; web plugin attached no results | Gemini's source tags were honest |

Where the lock gets the most pressure: item 2 (no clock, no take), the proposed slice (nothing to do), item 4 (edition layer, in-file licence).
